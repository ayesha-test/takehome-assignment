[pytest]
testpaths = tests
markers =
    performance: response-time smoke checks (may be slower on cold start)
    reliability: probes for intermittent platform or application errors
addopts = -ra --strict-markers -m "not reliability and not performance"
